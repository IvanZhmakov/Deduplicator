# Инструмент для дедупликации файлов

Deduplicator — это инструмент с графическим интерфейсом для поиска и удаления точных дубликатов файлов, особенно изображений в наборах пиксель-арт ресурсов. Полезен для подготовки данных для ML-пайплайнов, игр и других задач.

---

## Основные возможности

- Поиск дубликатов внутри одной папки или между несколькими папками.
- Визуальная проверка дубликатов: отображение пар файлов рядом.
- Динамический прогресс-бар с числом обработанных файлов и процентом выполнения.
- Запрет на выбор одинаковых папок.
- Проверка существования папок при ручном вводе и выборе через диалог.
- Удаление дубликатов с подтверждением пользователя.

---

## 🖥️ Интерфейс

- **Folder A, Folder B, Folder C:** папки, в которых будет производиться поиск дубликатов. Можно указать одну, две или три папки.
- **Start Deduplication:** кнопка запуска процесса поиска дубликатов.
- **Delete Duplicates from Selected Folder:** кнопка для удаления найденных дубликатов (с подтверждением).
- **Прогресс-бар:** отображает количество обработанных файлов и процент выполнения.
- **Detected duplicates:** список найденных дубликатов с указанием пар файлов.
- **Visual comparison:** визуальное сравнение выбранной пары файлов.

---

## 🛠️ Установка и запуск

1. Клонируйте репозиторий:
```bash
git clone https://github.com/IvanZhmakov/Deduplicator.git
cd Deduplicator
```

2. Создайте и активируйте виртуальное окружение:
```bash
python -m venv .venv
.venv\Scripts\activate
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Запустите приложение:
```bash
python main.py
```

---

## 📌 Примечания

- Если указана одна папка, поиск дубликатов происходит внутри неё.
- Если указаны несколько папок, поиск дубликатов происходит между ними.
- Программа проверяет существование папок и запрещает выбор одинаковых папок.
- Удаление дубликатов требует подтверждения пользователя.

---